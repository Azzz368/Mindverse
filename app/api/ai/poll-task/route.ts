import { NextResponse } from "next/server";
import { normalizeAIError } from "@/lib/ai/errors";
import { getAIProvider } from "@/lib/ai/provider";
const types = ["video", "audio", "image"] as const;
export async function POST(request: Request) { try { const body = await request.json() as { type?: unknown; taskId?: unknown }; if (!types.includes(body.type as typeof types[number]) || typeof body.taskId !== "string" || !body.taskId) return NextResponse.json({ ok: false, error: { message: "A valid task type and taskId are required.", code: "INVALID_REQUEST", status: 400 } }, { status: 400 }); const provider = getAIProvider(); if (!provider.pollTask) return NextResponse.json({ ok: false, error: { message: "This provider does not support task polling.", code: "POLLING_UNAVAILABLE", status: 400 } }, { status: 400 }); return NextResponse.json({ ok: true, provider: provider.name, output: await provider.pollTask(body.type as typeof types[number], body.taskId), polling: { intervalMs: Number(process.env.AI_302_POLL_INTERVAL_MS || 3000) } }); } catch (error) { const normalized = normalizeAIError(error); return NextResponse.json({ ok: false, error: normalized }, { status: normalized.status >= 400 && normalized.status < 600 ? normalized.status : 500 }); } }
